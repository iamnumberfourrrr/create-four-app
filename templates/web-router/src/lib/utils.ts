/**
 * Shared client-side utilities.
 * Phase 03 (Tailwind/shadcn) will add cn() helper here.
 */

/** Noop identity — placeholder until phase 03 adds real utils */
export function noop(): void {
  // intentional no-op
}
