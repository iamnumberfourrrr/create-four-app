# {{projectName}} — Setup guide

This file lists steps that the scaffolder couldn't fully automate. Each
section below covers one module you opted into. Skip sections for modules
you didn't pick.

Automation scripts (when present) are idempotent — re-running them is safe.

---
