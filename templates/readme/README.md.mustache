# {{projectName}}

Personal scaffold from create-four-app.

## What's inside

{{#tailwind}}- Tailwind v4{{#shadcn}} + shadcn/ui{{/shadcn}}
{{/tailwind}}{{#i18n}}- i18n (react-i18next)
{{/i18n}}{{#zustand}}- Zustand
{{/zustand}}{{#hasTanstack}}- TanStack: {{tanstackList}}
{{/hasTanstack}}{{#hasDb}}- Drizzle ORM ({{dbDialect}})
{{/hasDb}}{{#hasWorker}}- Background worker ({{workerImpl}})
{{/hasWorker}}{{#hasAuth}}- better-auth ({{authProviders}})
{{/hasAuth}}{{#docker}}- Docker (multi-target)
{{/docker}}{{#wrangler}}- Cloudflare Workers/Pages
{{/wrangler}}{{#ci}}- GitHub Actions CI (inert)
{{/ci}}
## Quick start

```bash
{{#hasComposeServices}}docker compose up -d
{{/hasComposeServices}}{{#hasDb}}pnpm db:push
{{/hasDb}}{{#hasAuth}}pnpm db:seed
{{/hasAuth}}pnpm dev
```

See [SETUP.md](./SETUP.md) for manual setup steps (Cloudflare account, OAuth
client IDs, Hyperdrive, etc.) — only sections for modules you picked appear.

## Stack

- {{frameworkName}} on Vite
- pnpm workspaces, no Turbo
- Oxlint + Oxfmt
- Vitest browser-mode (Playwright provider in CI){{#hasDb}}
- Drizzle ORM ({{dbDialect}}){{/hasDb}}{{#hasAuth}}
- better-auth ({{authProviders}}){{/hasAuth}}

## Scripts

| Command | Description |
|---|---|
| `pnpm dev` | Start dev servers |
| `pnpm build` | Build all packages |
| `pnpm test` | Run tests |
| `pnpm lint` | Lint with oxlint |
{{#hasDb}}| `pnpm db:push` | Push schema to DB |
| `pnpm db:generate` | Generate migration |
| `pnpm db:migrate` | Apply migrations |
| `pnpm db:studio` | Drizzle Studio |
{{/hasDb}}{{#hasAuth}}| `pnpm db:seed` | Seed dev user |
{{/hasAuth}}{{#docker}}| `pnpm docker:build` | Build images |
| `pnpm docker:up` | Start compose services |
{{/docker}}{{#wrangler}}| `pnpm deploy:cf` | Deploy to Cloudflare |
{{#hasHyperdrive}}| `pnpm cf:hyperdrive` | Setup Hyperdrive |
{{/hasHyperdrive}}{{/wrangler}}
## Project structure

```
{{projectName}}/
├── apps/
│   ├── web/
{{#hasWorker}}│   └── worker/
{{/hasWorker}}├── packages/
{{#hasDb}}│   ├── db/
{{/hasDb}}│   └── config/
{{#docker}}├── Dockerfile
{{/docker}}{{#hasComposeServices}}├── docker-compose.yml
{{/hasComposeServices}}{{#ci}}└── .github/
{{/ci}}```

---
Generated by [create-four-app](https://github.com/four/create-four-app).
